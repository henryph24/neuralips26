6/ ﻿The MoE community has framed
routing collapse as both an optimization problem (load balancing, z-loss all attack training dynamics;
effective when router input is informative but under-optimized) and a post-training representation
problem [55, 56], where token features cluster around expert centroids.  <- This way of writing makes it sound like this is an accusation. It may be us who haven't fully understood the domain just yet.Let's analyze this against a few papers with EXA-MCP. If this is correct I think we should also write a bit more conservatively here in this part.

---

Yes please

---

does it read less over-generaliseing and more conservative now ?

---

verify if the strong claims hold

---

5/ ﻿benchmarking work includes TEMPLATE [35] and GIFT-Eval [36]. All of these search over fine-﻿tuning strategies while keeping a single adapter architecture. We instead ask whether the adapter topology should be chosen per input window. <- Verify the "adpter topology" arguemnts with the literature, use Exa MCP to conduct more academic searches if needed
. I suspect this because maybe by design other TSFMs have used different adapter stragies, not just one static head. Why are we mentioning TEMPLATE here, what is it ?

---

8/ We should not mention the expert pool here, it reads out of context (artifacts from earlier expermental works). "﻿Question: Does the paper describe the usage of LLMs if it is an important, original, or
non-standard component of the core methods in this research? Note that if the LLM is used
only for writing, editing, or formatting purposes and does not impact the core methodology,
scientific rigor, or originality of the research, declaration is not required.
Answer: [N/A]
Justification: LLMs are not used as a component of the core methodology. The expert pool
consists of standard pooling heads (mean, last-token, max, attention, Conv1d) drawn from
the canonical sequence-representation literature."

---

/commit-push

---

"benchmark forecasting" is not a good term